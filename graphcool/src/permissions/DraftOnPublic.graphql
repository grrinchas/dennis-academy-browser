query isOwner($user_id: ID!, $draftDraft_id: ID!) {
    SomeUserExists(filter: {
        AND: [{
            id: $user_id
        }, {
            drafts_some: {
                id: $draftDraft_id
            }
        }
        ]
    })
}
