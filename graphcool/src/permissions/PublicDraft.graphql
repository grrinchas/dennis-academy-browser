
query isOwnerCreate($user_id: ID!, $input_draftId: ID!) {
    SomeUserExists(filter: {
        AND: [{
            id: $user_id
        }, {
            drafts_some: {
               id: $input_draftId
            }
        }]
    })
}
